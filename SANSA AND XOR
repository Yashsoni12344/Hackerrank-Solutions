int sansaXor(int arr_count, int* arr) {
    int result = 0;

    if (arr_count % 2 != 0) { // Only when n is odd
        for (int i = 0; i < arr_count; i += 2) { // Take even indices
            result ^= arr[i];
        }
    }
    return result; // If n is even, result stays 0
}
